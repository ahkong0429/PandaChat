cc.Class({
  extends: cc.Component,
  properties: {
  },
  onLoad() {
    // console.log(this.node.getContentSize());
    // console.log(this.node.height);
  },
  update(dt) {
    // console.log(this.node.getContentSize());
    // if (!this._preHeight) {
    //   this._preHeight = this.node.height;
    // }
    // if (this._preHeight !== this.node.height) {
    //   // todo
    //   // console.log(this.node.height);
    //   this.node.parent.height = this.node.height + 20;
    //   this.node.parent.getComponent(cc.Widget).top = this.node.parent.parent.getChildByName('tit').height;
    //   this._preHeight = this.node.height;
    // }
  }
});
